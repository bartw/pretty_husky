test("remove a semicolon and commit, you will see that prettier automatically inserts the semicolon.", () => {
    expect(4).toBe(4);
});

test("change one 4 to 5 and try to commit, this will not work.", () => {
    expect(4).toBe(4);
})
